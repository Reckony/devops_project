apiVersion: v1
kind: ConfigMap

metadata:
  name: my-app-configmap
  namespace: app
  labels:
    component: configmap
    app: myapp

data:
  PGUSER: "postgres"
  PGHOST: "mypostgres"
  PGPORT: "5432"
  PGDATABASE: "postgres"
  REDIS_HOST: "myredis"
  REDIS_PORT: "6379"
  BACKEND_PORT: "5000"